# macOS files
.DS_Store

node_modules

# in case a student adds an ssh key to the repo
*.pem

store
log.txt
.env
submission.zip

dist
stencil
.npm-cache

# generated deployment artifacts
shards/
all_keys.json

# local secrets / machine-specific deployment config
data/openai.key
aws-vars.sh
cluster.json
cluster.local.json
cluster.aws.json